#!/usr/bin/env python3
from __future__ import annotations

import sys
from pathlib import Path


def main() -> int:
    root = Path(__file__).resolve().parent.parent / "lib" / "kesk"
    sys.path.insert(0, str(root))
    sys.path.insert(0, str(root / "lib"))

    from gui.settings.main import main as gui_main

    return gui_main(sys.argv[1:], root)


if __name__ == "__main__":
    raise SystemExit(main())
